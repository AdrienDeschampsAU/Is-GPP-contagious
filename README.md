The file "Replication.do" is a Stata script that generates the econometric tables as in the manuscript. Please do not forget to change the repository path.

The files "survival" and "logit" contain the required data.
